import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import NumberDecomposer from './views/NumberDecomposer';

ReactDOM.render(
  <React.StrictMode>
    <NumberDecomposer />
  </React.StrictMode>,
  document.getElementById('root')
);